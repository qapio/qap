module Tdip.Qapio.Cron
import "Qapi.pkl"

typealias Stage = Qapi.Stage
typealias SourceStage = Qapi.SourceStage

function Cron(expression: String): SourceStage =
  new SourceStage {
    Endpoint = "Cron"
    Qapi = "From"
    Props = new Dynamic {
      Expression = expression
    }
  }